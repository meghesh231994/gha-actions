name: Hello world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "${{secrets.HELLO}}"

      
  # test:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Test phase
  #       run: echo "Running a test"

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - name: Deploy the app
  #       run: echo "Deploying the app"

        
    
        
        
