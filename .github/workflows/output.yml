name: output demo
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step one
        id: step1
        run: echo "version =0.0.1" >> $GTIHUB_OUTPUT
      - name: Step two
        run: echo "The version is ${{steps.step1.outputs.version}}"
        
